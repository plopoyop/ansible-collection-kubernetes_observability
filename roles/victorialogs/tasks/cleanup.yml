---
- name: "Remove VictoriaLogs single instance"
  kubernetes.core.helm:
    name: "{{ victorialogs_deployment_name }}"
    namespace: "{{ victorialogs_namespace }}"
    chart_ref: "{{ victorialogs_helm_chart_ref }}"
    state: "absent"
    wait: true
  tags:
    - "helm_chart"

- name: "Pause for job to complete."
  ansible.builtin.pause:
    seconds: 30

- name: "Remove VictoriaLogs namespace"
  kubernetes.core.k8s:
    name: "{{ victorialogs_namespace }}"
    api_version: "v1"
    kind: "Namespace"
    state: "absent"
  tags:
    - "namespace"
